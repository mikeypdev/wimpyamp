name: CI/Release

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main ]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.13
      uses: actions/setup-python@v4
      with:
        python-version: "3.13"

    - name: Run quality checks via Makefile
      run: |
        make setup
        make check

  build:
    needs: check
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            arch: arm64
          - os: macos-15-intel
            arch: x86_64
          - os: windows-latest
            arch: x86_64
          - os: ubuntu-latest
            arch: x86_64
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.13
      uses: actions/setup-python@v4
      with:
        python-version: "3.13"

    - name: Create distribution
      run: |
        make setup
        ARCH=${{ matrix.arch }} make dist-archive

    - name: Create signed and notarized distribution (tags only)
      if: matrix.os == 'macos-latest' || matrix.os == 'macos-15-intel'
      run: |
        make setup
        ARCH=${{ matrix.arch }} make dist-archive NOTARIZE=true
      env:
        APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
        APPLE_ID: ${{ secrets.APPLE_ID }}
        APPLE_ID_PASSWORD: ${{ secrets.APPLE_ID_PASSWORD }}
        APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: WimPyAmp-${{ matrix.os }}-${{ matrix.arch }}
        path: |
          dist/*.dmg
          dist/*.tar.gz
          dist/*.zip
        if-no-files-found: error

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - name: Download Artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts
        merge-multiple: true

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        draft: true
        files: artifacts/*
        name: Release ${{ github.ref_name }}
        body: |
          Automated release for version ${{ github.ref_name }}.
          
          This release contains builds for macOS (ARM64 & x86_64), Windows (x86_64), and Linux (x86_64).
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
